# Git과 GitHub — 직접 쓰는 사람을 위한 안내

개발이 전문이 아니지만 이 프로젝트의 파일을 직접 고치고 올릴 사람을 위한 글이다.
명령어를 외우게 하려는 게 아니라, **지금 무엇을 해야 하는지 스스로 판단할 수 있게** 하는 것이 목적이다.

용어만 빠르게 찾고 싶다면 [기술 용어 설명](glossary.md)을 보면 된다.

---

## 1. Git이 없으면 무슨 일이 벌어지나

한 번쯤 이런 폴더를 본 적이 있을 것이다.

```
   미션3_최종.html
   미션3_최종_수정.html
   미션3_진짜최종.html
   미션3_진짜최종_v2_이걸로.html
```

이 방식의 문제는 파일이 늘어나는 게 아니라 이것이다.

- **무엇이 왜 바뀌었는지 남지 않는다.** 두 파일을 열어 눈으로 비교해야 한다.
- **되돌릴 수가 없다.** 어제 것이 맞았는데, 어느 파일이 어제 것인지 모른다.
- **둘이 같이 작업하면 한쪽이 덮인다.** 누가 언제 덮었는지도 모른다.

**Git은 이 세 가지를 해결하는 도구다.** 파일을 복사해 두는 대신, *"언제 · 누가 · 무엇을 · 왜 바꿨는지"* 를
시점마다 기록해 두고 **언제든 그 시점으로 되돌아갈 수 있게** 한다.

## 2. Git과 GitHub는 다른 것이다

둘을 한 덩어리로 알고 있으면 계속 헷갈린다. **다른 물건이다.**

|              | Git                        | GitHub                              |
| ------------ | -------------------------- | ----------------------------------- |
| 무엇인가     | 기록을 남기는 **도구**      | 그 기록을 올려 두고 **함께 쓰는 곳** |
| 어디에 있나  | **내 컴퓨터** 안            | **인터넷** 위                        |
| 없으면       | 기록 자체가 안 된다         | 혼자서는 쓸 수 있다                  |
| 비유         | 사진을 찍는 **카메라**      | 사진을 모아 두는 **공유 앨범**       |

그래서 **"기록을 남기는 것"과 "남들이 볼 수 있게 올리는 것"은 서로 다른 동작**이다.
이 구분이 다음 절의 핵심이다.

## 3. 네 가지 동작 — commit · push · pull · PR

```
   1.  파일을 고친다
   2.  commit         내 컴퓨터의 기록에 "여기까지 했다"고 남긴다
   3.  push    -->    GitHub 에 올린다. 이때부터 남이 볼 수 있다
       pull    <--    남이 올린 것을 내 컴퓨터로 받아온다
   4.  PR      -->    "내 가지를 합쳐 주세요" 하고 요청한다
```

**commit (커밋) — 기록에 남기기**
지금까지 고친 것을 한 덩어리로 묶어 기록에 남긴다. 사진 한 장 찍는 것과 같다.
찍을 때 **메모(커밋 메시지)** 를 같이 남기는데, 나중에 되돌아볼 때 이 메모가 전부다.
*"수정"* 말고 *"미션3 대사 줄바꿈 정리"* 처럼 **무엇을 왜 바꿨는지** 쓴다.

**push (푸시) — GitHub에 올리기**
내 컴퓨터에 쌓인 커밋들을 GitHub로 올린다. **push 하기 전까지는 나만 갖고 있는 것**이다.

> **가장 흔한 오해 — "커밋했는데 왜 안 보이죠?"**
> 커밋은 **내 컴퓨터 안**에 기록한 것이다. **push 를 해야** GitHub에 올라가고 남이 볼 수 있다.
> 사진을 찍은 것(commit)과 앨범에 올린 것(push)은 다르다.

**pull (풀) — GitHub에서 받아오기**
남이 올려 둔 변경을 내 컴퓨터로 가져온다.
**작업을 시작하기 전에 습관처럼 한 번** 하는 게 좋다. 옛날 것 위에 작업하면 나중에 꼬인다.

**PR (Pull Request, 풀 리퀘스트) — 합쳐 달라고 요청하기**
내가 따로 작업한 가지를 원본(main)에 **합쳐 달라고 요청하는 자리**다.
그냥 합치지 않고 PR을 거치는 이유는, **합치기 전에 무엇이 어떻게 바뀌는지 한눈에 보여 주고
확인받을 수 있기 때문**이다. 이 프로젝트는 지금까지 **148개의 PR**을 거쳐 만들어졌다.

## 4. 브랜치 — 가장 헷갈리는 것

### 한 줄로 말하면

브랜치(가지) = **원본을 건드리지 않고 따로 갈라 둔 작업용 사본.**

```
   main         o-----o-----o-------------------o-->
                             \                 /
   feat/mission3              o----o----o------
                            (1)               (2)
```

- **(1) 가지 따기** — 지금 main 의 상태를 그대로 복사해 옆으로 갈라 나온다.
  이 순간부터 내가 무엇을 하든 **main 은 아무 영향을 받지 않는다.**
- **(2) 합치기** — 작업이 끝나면 PR을 올리고, 확인을 거쳐 main 에 들어간다.

### 왜 굳이 따로 떼서 작업하나

1. **망쳐도 원본이 멀쩡하다.** 잘못됐으면 그 가지를 버리면 그만이다. main 은 처음부터 그대로다.
2. **여럿이 동시에 일할 수 있다.** 각자 다른 가지에서 작업하면 서로의 작업을 덮어쓰지 않는다.
3. **완성되기 전 모습이 남에게 보이지 않는다.** 반쯤 만든 미션이 main 에 섞이지 않는다.

정리하면, 브랜치는 **"실패해도 되는 공간"** 을 만들어 주는 장치다.
main 은 언제나 동작하는 상태로 두고, 위험한 일은 전부 가지에서 한다.

### 이 프로젝트가 미션을 만드는 방식과 똑같다

이 게임은 **미션 하나를 프로젝트 밖에서 따로 만든 뒤, 완성되면 폴더째 가져와 붙인다.**
그래야 미션3을 만들다 실수해도 잘 돌던 미션1이 망가지지 않는다.
([왜 그렇게 했는지](faq.md))

브랜치도 발상이 완전히 같다. **본체를 건드리지 않고 따로 작업하다, 완성되면 붙인다.**
이미 익숙한 방식이 코드 관리에도 그대로 적용된 것뿐이다.

### 이 프로젝트의 규칙

- **main 에 직접 커밋하지 않는다.** 무슨 작업이든 가지를 먼저 딴다.
- **작업 하나 = 가지 하나.** 여러 작업을 한 가지에 섞지 않는다. 섞이면 PR에서 확인하기 어렵다.
- **가지 이름은 하는 일에 따라 붙인다.**

  | 앞머리   | 언제                | 예시                          |
  | -------- | ------------------- | ----------------------------- |
  | `feat/`  | 새 기능 · 새 화면    | `feat/planet3-minimap-label`  |
  | `fix/`   | 잘못된 것 고치기     | `fix/home-final-farewell`     |
  | `chore/` | 정리 · 잡일          | `chore/remove-unused-bgm`     |
  | `docs/`  | 문서 작업            | `docs/resource-credits`       |

- **합쳐진 가지는 지운다.** 역할이 끝났으므로 남겨 두면 목록만 지저분해진다.

## 5. 한 번의 작업, 처음부터 끝까지

미션3의 대사 한 줄을 고친다고 해 보자.

| 순서 | 무엇을                    | 왜                                          |
| ---- | ------------------------- | --------------------------------------------- |
| 1    | **pull** 로 최신 상태 받기 | 남이 올린 변경 위에서 시작해야 나중에 안 꼬인다 |
| 2    | **가지 따기** (`fix/...`)  | main 을 건드리지 않기 위해                     |
| 3    | 파일 고치기                |                                               |
| 4    | 확인해 보기                | 고친 게 실제로 동작하는지                      |
| 5    | **commit** (메모와 함께)   | 여기까지 했다고 기록에 남긴다                  |
| 6    | **push**                   | GitHub 에 올려 남이 볼 수 있게                 |
| 7    | **PR 올리기**              | 무엇이 바뀌는지 보여 주고 합쳐 달라고 요청     |
| 8    | 확인 후 합치기 · 가지 삭제 |                                               |

3~5번은 여러 번 반복해도 된다. **작업이 한 덩어리 끝날 때마다 커밋**하는 편이,
하루치를 한 번에 커밋하는 것보다 나중에 되돌리기 쉽다.

## 6. 같은 일을 부르는 세 가지 이름

도구마다 버튼 이름이 다를 뿐 하는 일은 같다.

| 하는 일          | 명령어 (터미널)              | GitHub Desktop            | VS Code                    |
| ---------------- | ---------------------------- | ------------------------- | -------------------------- |
| 가지 따기        | `git switch -c feat/이름`    | Current Branch → New Branch | 좌측 하단 브랜치 이름 클릭 |
| 고친 것 고르기   | `git add .`                  | 체크박스 (자동)            | 변경 목록의 `+`            |
| 기록에 남기기    | `git commit -m "메모"`       | Commit to <가지>          | 메시지 입력 후 ✓           |
| GitHub 에 올리기 | `git push`                   | Push origin               | ··· → Push                 |
| 받아오기         | `git pull`                   | Pull origin               | ··· → Pull                 |
| PR 올리기        | `gh pr create`               | Preview Pull Request      | GitHub 웹에서              |

처음에는 **GitHub Desktop 같은 화면 도구로 시작해도 전혀 문제없다.**
안에서 벌어지는 일은 명령어와 똑같다.

## 7. 자주 막히는 곳

| 증상                                 | 원인과 해결                                                        |
| ------------------------------------ | -------------------------------------------------------------------- |
| "올렸는데 GitHub에 안 보여요"        | commit 만 하고 **push 를 안 했다.** push 하면 보인다                   |
| "남이 바꾼 게 제 화면엔 없어요"      | **pull 을 안 했다.** 작업 시작 전에 pull 하는 습관을 들이면 된다        |
| "실수로 main 에서 작업해 버렸어요"   | 옮길 수 있다. **혼자 되돌리려 하지 말고 물어볼 것**                     |
| "충돌(conflict) 났어요"              | 에러가 아니다. 같은 줄을 둘이 다르게 고쳤으니 **어느 쪽을 남길지 골라 달라**는 요청이다 |
| "잘못된 걸 올려 버렸어요"            | git 은 지우는 것보다 **되돌리는 것**이 안전하다. 기록은 남겨 두고 고치면 된다 |

마지막 두 가지는 **혼자 해결하려다 더 꼬이는 경우가 많다.** 막히면 물어보는 편이 빠르다.
Git 은 한 번 기록된 것을 웬만해선 잃지 않으므로, **거의 모든 실수는 되돌릴 수 있다.**

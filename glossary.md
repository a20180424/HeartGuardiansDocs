# 기술 용어 설명

[기술 및 서비스 구성](tech-stack.md)과 [FAQ](faq.md)에 나오는 용어를 개발 지식 없이도 알 수 있게 풀어 썼다.
각 항목은 **용어 — 일반적인 뜻**, 그 아래 **→ 이 프로젝트에서** 어떻게 쓰였는지 순서다.

## 표기 보는 법

용어 앞의 마크는 **발표에서의 중요도(★)** 와 **이해 난이도(신호등)** 다.

| 중요도 | 뜻                                                              | 개수 |
| ------ | ---------------------------------------------------------------- | ---- |
| ★★★    | 발표 중 직접 입에 올릴 말. 이것만 알아도 발표를 따라갈 수 있다.   | 20   |
| ★★☆    | 질문이 나오면 답해야 할 말.                                       | 22   |
| ★☆☆    | 문서에만 나오는 말. 몰라도 발표에는 지장 없다.                    | 18   |

| 난이도 | 뜻                                                          | 개수 |
| ------ | ------------------------------------------------------------- | ---- |
| 🟢     | 한 번 읽으면 바로 이해된다.                                   | 34   |
| 🟡     | 설명을 한 번 더 곱씹어야 한다.                                | 22   |
| 🔴     | 개발 배경지식이 있어야 정확히 이해된다. 넘어가도 좋다.        | 4    |

**개발 배경이 없다면 ★★★ 20개만 읽어도 충분하다.**
★☆☆ 🔴 가 붙은 항목은 개발자용이니 건너뛰어도 발표를 이해하는 데 아무 지장이 없다.

> **발표 중에 딱 5개만 설명해야 한다면**
> **웹앱**(브라우저 기술로 만든 앱) · **Capacitor**(웹앱을 태블릿 앱으로 포장) ·
> **API**(앱과 데이터베이스 사이의 창구) · **서버리스**(서버를 직접 빌리지 않는 방식) ·
> **RLS**(데이터베이스가 직접 권한을 판단하는 기능)

---

## 1. 웹 앱을 만드는 기술

★★★ 🟢 **HTML** — 화면에 무엇이 있는지를 적는 언어. 제목·버튼·그림의 **뼈대**.

★★★ 🟢 **CSS** — 그것들이 어떻게 보일지를 적는 언어. 색·크기·위치 같은 **겉모습**.

★★★ 🟢 **JavaScript** — 화면이 어떻게 움직일지를 적는 언어. 버튼을 누르면 벌어지는 일 같은 **동작**.
→ 이 셋이 웹페이지를 이루는 기본 3종 세트다. 이 앱은 이 셋만으로 만들어졌다.

★★★ 🟡 **프레임워크 (Framework)** — 앱을 빠르게 만들도록 미리 짜여 있는 뼈대와 규칙 모음. React·Vue가 대표적이다.
정해진 틀을 따르면 편하지만, 그 틀을 배우고 따라야 한다.
→ 이 프로젝트는 **쓰지 않았다.** 화면마다 독립적으로 만들어 서로 영향을 주지 않게 하려는 의도였다.

★★★ 🟡 **빌드 (Build) / 번들러 (Bundler)** — 사람이 쓴 코드를 브라우저가 읽기 좋은 형태로 **변환·압축하는 과정**과 그 도구(Vite·webpack).
보통의 웹 개발은 코드를 고친 뒤 이 과정을 거쳐야 실행물이 나온다.
→ 이 프로젝트는 **빌드 단계가 없다(무빌드).** 작성한 파일이 그대로 실행된다.

★★☆ 🟢 **정적 파일 (Static file)** — 서버가 계산해서 만들어 내는 게 아니라, 이미 만들어져 있어 그대로 내려 주기만 하면 되는 파일.
→ 이 앱은 전부 정적 파일이라 호스팅이 단순하고 빠르다.

★★☆ 🟡 **MPA (Multi-Page Application)** — 화면마다 **별도의 페이지 파일**이 있고, 이동할 때 페이지를 새로 여는 방식.
**SPA (Single-Page Application)** 는 반대로 페이지 하나를 띄워 두고 내용만 갈아 끼우는 방식이다.
→ 이 앱은 MPA다. 미션 하나가 곧 폴더 하나이고, 한 미션을 고쳐도 다른 미션에 영향이 없다.

★☆☆ 🟡 **상대경로 / 절대경로** — 파일 위치를 **"지금 위치에서 몇 칸 옆"** 으로 적는 방식(상대)과 **"맨 처음부터 전체 주소"** 로 적는 방식(절대).
→ 이 앱은 전부 상대경로라, 어느 주소에 올려도 링크가 깨지지 않는다.

## 2. 화면을 풍성하게 만드는 브라우저 기능

★★★ 🟢 **three.js** — 웹페이지에 3D 그래픽을 그릴 수 있게 해 주는 라이브러리.
→ 행성3의 3D 우주 공간이 이걸로 만들어졌다.

★☆☆ 🟢 **glTF / .glb** — 3D 모델(캐릭터·사물)을 담는 파일 형식. 3D 세계의 "그림 파일"에 해당한다.
이 파일을 읽어 들이는 코드를 **GLTFLoader**라고 부른다.
→ NPC 캐릭터들이 `.glb` 파일로 들어 있다.

★★☆ 🟢 **Web Audio API** — 브라우저가 소리를 재생하고 **직접 만들어 내기도** 하는 기능.
→ 효과음 재생과 배경 앰비언스(잔잔한 우주 소리) 합성에 썼다.

★☆☆ 🟢 **Canvas** — 웹페이지 안에서 그림을 자유롭게 그릴 수 있는 **도화지 역할의 요소**.
→ 수료증 이미지를 이 위에 그려서 만든다.

★★☆ 🟡 **Web Worker** — 무거운 계산을 **화면과 분리된 뒷줄에서** 처리하는 기능. 화면이 멈추지 않는다.
→ 수료증을 이미지 파일로 굽는 작업에 썼다. 화면에서 하면 4초 멈추던 것이 0.07초로 줄었다.

★★☆ 🟢 **localStorage** — 브라우저(앱) 안에 작은 메모를 남겨 두는 저장 공간. 앱을 껐다 켜도 남아 있다.
→ 로그인 상태·진도·음소거 설정을 여기에 보관한다.

## 3. 웹앱을 태블릿 앱으로

★★☆ 🟢 **WebView** — 앱 안에 들어 있는 **브라우저 화면**. 주소창 같은 게 없는 브라우저라고 보면 된다.

★★★ 🟢 **Capacitor** — 웹앱을 WebView에 담아 **안드로이드·iOS 앱으로 설치되게** 포장해 주는 도구.
→ 이 프로젝트가 웹 기술로 만든 게임을 태블릿 앱으로 만들 수 있었던 핵심.

★★★ 🟢 **APK** — 안드로이드 **앱 설치 파일**. 윈도우의 `.exe` 같은 것.
→ 태블릿에 설치하는 최종 결과물이 이 형태다.

★☆☆ 🟢 **webDir** — Capacitor에게 "앱으로 포장할 폴더가 여기다"라고 알려 주는 설정.
→ 이 프로젝트는 `www` 폴더로 지정돼 있다.

★★☆ 🟡 **네이티브 플러그인 (Native plugin)** — 웹 기술만으로는 못 하는 일(기기 기능 사용)을 대신해 주는 **안드로이드 쪽 부품**.
→ 수료증을 갤러리에 저장하는 기능은 웹만으로 불가능해서 직접 만들어 붙였다(MediaStore 사용).

★☆☆ 🟢 **minSdk** — 이 앱이 지원하는 **가장 낮은 안드로이드 버전**. 너무 오래된 기기는 설치되지 않는다.

★☆☆ 🔴 **DPR (Device Pixel Ratio)** — 화면의 **실제 점 개수와 화면이 스스로 말하는 크기의 비율**.
태블릿은 물리적으로 1920×1200이지만 DPR이 1.5라 앱은 1280×800으로 계산한다.

## 4. 서버 — API

★★★ 🟢 **API** — 프로그램끼리 주고받는 **정해진 창구**. 앱이 "이 학생 진도 알려 줘"라고 물으면 답해 주는 곳.
→ 앱이 데이터베이스에 직접 붙지 않고 이 창구를 통하게 했다.

★★☆ 🟡 **REST / 엔드포인트 (Endpoint)** — API를 **주소 하나에 기능 하나**씩 붙여 만드는 방식과, 그 개별 주소.
→ `/api/progress`는 진도, `/api/schools`는 학교 목록을 담당하는 식이다.

★★☆ 🟡 **HTTP 헤더 (Header)** — 요청을 보낼 때 본문과 별도로 붙이는 **쪽지**. 누가 보냈는지 같은 정보를 담는다.
→ 학생 로그인 정보를 이 쪽지에 담아 보낸다.

★☆☆ 🔴 **CORS** — 어떤 웹사이트가 **다른 주소의 서버를 불러도 되는지** 브라우저가 검사하는 규칙.
→ 앱과 API 주소가 다르므로 API에서 허용 설정을 해 두었다.

★★★ 🟡 **서버리스 (Serverless)** — 서버가 없다는 뜻이 **아니라**, 서버를 직접 빌리고 관리하지 않는 방식.
요청이 올 때만 코드가 실행되고, 쓰지 않으면 비용도 들지 않는다.
→ 상시 켜 둘 서버 없이 API를 운영하고 있다.

★★★ 🟢 **Cloudflare Workers** — Cloudflare가 제공하는 서버리스 실행 환경. 코드를 올려 두면 알아서 돌려 준다.
→ API 서버가 여기서 돌아간다.

★☆☆ 🔴 **V8 / isolate** — **V8**은 구글 크롬에 들어 있는 JavaScript 실행 엔진이고,
**isolate**는 그 엔진이 코드마다 따로 만들어 주는 **아주 가벼운 격리된 실행 공간**이다.
서버 한 대를 통째로 띄우는 것보다 훨씬 가볍고 빨리 시작된다.
→ Cloudflare Workers가 이 방식으로 동작한다. "서버를 켜 두지 않아도 즉시 응답"이 가능한 이유다.

★★☆ 🟡 **엣지 (Edge)** — 사용자와 **지리적으로 가까운** 서버. 멀리 있는 한 곳까지 가지 않아도 되니 응답이 빠르다.
→ Workers는 요청이 들어온 곳과 가까운 엣지에서 실행된다.

★★★ 🟢 **Cloudflare Pages** — Cloudflare의 **정적 사이트 호스팅** 서비스.
→ 앱의 웹 버전이 여기에 올라가 있다.

★★☆ 🟢 **TypeScript** — JavaScript에 **"이 값은 숫자다, 글자다"** 하는 표시를 붙일 수 있게 한 언어.
실수를 미리 잡아 준다.
→ API 서버 코드가 TypeScript로 쓰여 있다.

★★★ 🟢 **Hono** — API를 만들 때 쓰는 **가벼운 웹 프레임워크**. 주소별로 할 일을 연결해 준다.
→ API의 뼈대다. (Python 계열인 FastAPI와 자주 혼동되는데, Hono는 JavaScript 계열이다.)

★★☆ 🟡 **OpenAPI / Swagger UI** — API에 **어떤 창구가 있고 무엇을 주고받는지** 적은 규격 문서(OpenAPI)와,
그걸 웹페이지로 보기 좋게 보여 주는 도구(Swagger UI).
→ 코드에서 자동으로 만들어져 `/api/docs`에서 볼 수 있다.

★☆☆ 🟡 **zod** — 들어온 데이터가 **형식에 맞는지 검사**해 주는 도구. 잘못된 요청을 미리 막는다.

★☆☆ 🟢 **vitest** — 코드가 의도대로 동작하는지 **자동으로 검사**해 주는 테스트 도구.

★☆☆ 🟢 **wrangler** — Cloudflare Workers에 코드를 **올리는(배포하는) 명령어 도구**.

## 5. 데이터베이스

★★★ 🟢 **데이터베이스 (Database, DB)** — 데이터를 **정리해서 보관하고 빠르게 찾을 수 있게** 해 주는 저장소.

★★★ 🟡 **관계형 데이터베이스 (RDBMS)** — 데이터를 **표(테이블) 형태**로 저장하고, 표끼리 서로 연결해 관리하는 방식.
엑셀 시트 여러 장이 서로 참조하고 있는 모습에 가깝다.

★★★ 🟢 **PostgreSQL** — 널리 쓰이는 관계형 데이터베이스 제품 중 하나.
→ 이 프로젝트의 모든 데이터가 여기에 있다.

★★☆ 🟢 **테이블 / 행 (Row)** — 표 하나가 테이블, 그 안의 줄 하나가 행이다. 학생 한 명이 `profiles` 테이블의 한 행이다.

★★★ 🟢 **Supabase** — 데이터베이스·로그인·API를 **한 번에 제공해 주는 서비스**.
직접 서버를 세우고 관리하지 않아도 되게 해 준다.

★★☆ 🟡 **관리형 서비스 (Managed service)** — 설치·백업·보안 업데이트 같은 관리를 **제공 업체가 대신 해 주는** 형태.
→ Supabase가 데이터베이스를, Cloudflare가 서버를 대신 관리한다.

★☆☆ 🔴 **PostgREST** — PostgreSQL 데이터베이스를 **웹에서 바로 부를 수 있는 API 형태로 열어 주는** 기능.
→ 선생님 콘솔이 이걸 통해 데이터베이스와 직접 이야기한다.

★☆☆ 🟡 **UUID** — 전 세계에서 겹치지 않도록 만든 **긴 고유 번호**. 학교마다 하나씩 붙어 있다.

★☆☆ 🟡 **트리거 (Trigger)** — 특정 일이 벌어지면 데이터베이스가 **자동으로 실행하는 동작**.
→ 선생님이 가입하면 `teachers` 표에 줄이 자동으로 하나 생긴다.

## 6. 인증과 보안

★★★ 🟢 **인증 (Authentication)** — **"당신이 누구인지"** 확인하는 절차. 로그인이 여기에 해당한다.

★★☆ 🟡 **토큰 (Token)** — 로그인에 성공했다는 **출입증**. 이후 요청에는 비밀번호 대신 이 출입증을 보여 준다.
- **access token** — 실제로 쓰는 출입증. 보안을 위해 한 시간쯤이면 만료된다.
- **refresh token** — 만료된 출입증을 **새로 발급받기 위한 교환권**.
→ 선생님·운영자 로그인이 이 방식이다. 학생은 토큰 없이 매번 자기 정보를 보낸다.

★★★ 🟡 **RLS (Row Level Security, 행 수준 보안)** — 데이터베이스가 **줄 하나하나마다 "이 사람이 이걸 봐도 되는가"를 직접 판단**하는 기능.
프로그램이 실수로 남의 데이터를 요청해도 데이터베이스가 거부한다.
→ 선생님은 자기 반 학생만 보이는데, 그걸 화면이 아니라 **데이터베이스가 보장한다.**

★★☆ 🟡 **service_role 키 / publishable 키** — 데이터베이스에 접근할 때 쓰는 열쇠 두 종류.
`service_role`은 **모든 걸 할 수 있는 마스터 키**라 서버에만 두고, `publishable`은 **공개돼도 되는 열쇠**로
브라우저에서 쓴다. 공개 열쇠로 할 수 있는 일의 범위는 위의 RLS가 정한다.

## 7. 개발·배포 도구

★★★ 🟡 **Node.js** — JavaScript를 **브라우저 밖에서도** 실행할 수 있게 해 주는 프로그램.
→ 이 프로젝트에서는 서버가 아니라 **개발 도구를 돌리는 용도**로만 쓴다.

★★☆ 🟢 **npm** — Node.js용 **부품(라이브러리) 관리 도구**. 필요한 것을 내려받고 버전을 관리한다.

★☆☆ 🟢 **browser-sync** — 개발 중 파일을 고치면 **브라우저가 자동으로 새로고침**되게 해 주는 도구.

★☆☆ 🟡 **Gradle / adb** — 안드로이드 앱을 **빌드하는 도구**(Gradle)와 PC에서 **태블릿을 조작하는 명령어 도구**(adb).
→ APK를 만들어 기기에 설치하는 과정에 쓰인다.

★★☆ 🟢 **Git / GitHub / 레포지토리(Repo)** — 코드의 **변경 이력을 기록하는 도구**(Git),
그걸 온라인에 보관·공유하는 서비스(GitHub), 그리고 하나의 프로젝트 보관함(레포지토리).

★☆☆ 🟢 **브랜치 (Branch)** — 원본을 건드리지 않고 **작업용으로 갈라 둔 가지**. 다 되면 원본에 합친다.
`gh-pages`는 GitHub Pages로 내보낼 파일만 담아 두는 관례적인 브랜치 이름이다.
→ 직접 git 을 쓸 사람은 [Git과 GitHub 안내](git-basics.md)에 그림과 함께 자세히 있다.

★☆☆ 🟡 **GitHub Actions** — 코드를 올리면 **자동으로 검사·배포를 실행**해 주는 GitHub의 기능.

★★☆ 🟢 **호스팅 (Hosting)** — 만든 웹사이트를 **인터넷에 올려 두고 접속할 수 있게** 해 주는 것.

★★☆ 🟢 **CDN (Content Delivery Network)** — 파일 사본을 **전 세계 여러 곳에 미리 뿌려 두고**
접속자와 가까운 곳에서 내려 주는 배달망. 그래서 빠르다.

★★☆ 🟢 **대역폭 (Bandwidth)** — 일정 기간에 **오간 데이터의 총량**. 호스팅 서비스는 보통 월 단위로 제한을 둔다.

★★☆ 🟢 **배포 (Deploy)** — 만든 코드를 **실제로 동작하는 자리에 올리는 것**.
→ API는 코드를 GitHub에 반영하면 자동으로 배포된다.
→ 배포가 왜 필요한지는 [내 컴퓨터 밖으로 — 배포와 서버](deploy-and-server.md)에 자세히 있다.

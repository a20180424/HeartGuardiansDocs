# 내 컴퓨터 밖으로 — 배포와 서버

바이브 코딩으로 무언가 만들어 본 다음 **곧바로 부딪히는 두 가지 벽**을 설명한다.

1. "이걸 남한테 어떻게 보여주지?"
2. "로그인이나 점수 저장은 어떻게 하지?"

둘은 따로 떨어진 문제처럼 보이지만 사실 하나로 이어져 있다.

> **내 컴퓨터 밖에 무엇을 둘 것인가.**

---

## 1. 여기까지는 왔다

HTML 파일을 하나 만들었다. 더블클릭하면 브라우저에 화면이 뜬다.
버튼을 누르면 반응도 한다.

많은 사람이 여기서 "바이브 코딩이 이런 거구나" 한다. 맞다.
다만 **진짜 문제는 여기서부터 시작된다.**

## 2. 첫 번째 벽 — "친구한테 보여주고 싶은데"

### 파일을 보내면 되지 않나?

해 보면 금방 막힌다.

- **이미지가 안 나온다.** 화면은 파일 하나가 아니라 여러 파일이 얽혀 있다. 폴더째 보내야 한다
- **고칠 때마다 다시 보내야 한다.** 상대는 옛날 것을 보고 있다
- **상대가 받아서 압축을 풀고 파일을 찾아 열어야 한다**
- **휴대폰에서는 잘 열리지 않는다**

원하는 건 이게 아니다. 원하는 건 이것이다.

> **주소 하나를 보내면, 누르면 바로 열리는 것.**

### 그러려면 무엇이 필요한가

상대가 그 주소를 누르는 순간, **누군가는 그 파일을 건네줘야 한다.**
내 컴퓨터가 꺼져 있어도 말이다.

즉 **24시간 켜진 채로 인터넷에 붙어 있는 컴퓨터**가 있어야 한다.
그게 서버다.

### 서버는 멀리 있는 무언가가 아니다

"서버"라는 말을 들으면 어딘가 먼 곳의 거대한 기계를 떠올리기 쉽다. 그렇지 않다.

> **서버 = 꺼지지 않고 인터넷에 연결된 채, 요청이 오면 응답하는 컴퓨터.**
> 그게 전부다. 특별한 기계가 아니다.

증거가 있다. **지금 쓰는 노트북도 서버가 될 수 있고, 실제로 그렇게 쓰고 있다.**
이 프로젝트에서 `npm run dev` 를 치면 내 컴퓨터가 서버가 된다.
브라우저에 `localhost` 를 치면 **내 컴퓨터가 내 요청에 응답한다.**

그런데 내 노트북에는 문제가 있다.

- 덮으면 꺼진다
- 집 인터넷 주소는 계속 바뀐다. 오늘 알려 준 주소가 내일은 안 된다
- 24시간 켜 두기 어렵고, 보안도 신경 써야 한다

그래서 **항상 켜져 있는 남의 컴퓨터를 빌린다. 그게 클라우드다.**
Cloudflare · Supabase 같은 이름들이 전부 "컴퓨터를 빌려주는 회사"다.

### 배포 — 올려 두고 주소를 받는 일

**배포(deploy)** 는 어려운 말이 아니다.

> **내가 만든 파일을 그 빌린 컴퓨터에 올려 두고, 주소를 받는 일.**

| | 파일을 보낼 때        | 배포했을 때                 |
| --- | --------------------- | --------------------------- |
| 보내는 것 | 파일 뭉치             | **주소 한 줄**              |
| 받는 쪽 | 받아서 풀고 열어야 한다 | **누르면 바로 열린다**      |
| 고쳤을 때 | 다시 보내야 한다       | **그 주소가 이미 최신이다** |
| 휴대폰 | 잘 안 된다             | 된다                        |

## 3. 서버가 하는 두 가지 일 — 여기서 헷갈린다

이 문서에서 가장 조심해야 할 대목이다.
**"서버"라는 한 단어가 서로 다른 두 가지를 가리키는 것처럼 쓰이기 때문이다.**

정리하면 이렇다. **서버라는 말의 뜻은 하나다.** 위에서 본 그대로 —
꺼지지 않고 요청에 응답하는 컴퓨터. 헷갈리는 이유는 **그 서버가 하는 일이 두 종류**라서다.

| 서버가 하는 일        | 무엇을 하나                          | 요청하는 사람이 달라지면 | 흔히 부르는 이름     |
| --------------------- | ------------------------------------- | ------------------------ | -------------------- |
| **파일을 건네주는 일** | 이미 만들어 둔 파일을 그대로 내려 준다 | **누가 와도 같은 것**     | 호스팅               |
| **물음에 답하는 일**   | 요청을 받아 그때그때 답을 만든다       | **사람마다 다른 답**      | API 서버 · 백엔드    |

가르는 기준은 한 문장이면 된다.

> **누가 와도 같은 것을 주는가, 묻는 사람마다 답이 달라지는가.**

이 게임 안에서 두 일이 동시에 벌어진다.

- 게임 화면 · 그림 · 소리 · 3D 모델 → **모든 학생이 똑같은 것**을 받는다 → 파일을 건네주는 일
- "내 진도가 어디까지지?" → **학생마다 다른 답**이 나온다 → 물음에 답하는 일

### "프론트엔드 서버"라는 말에 속지 않기

흔히 쓰는 말이지만 두 가지가 섞인 표현이다. 실제로는 셋이 다르다.

| 이름         | 어디서 도는가                                    |
| ------------ | ------------------------------------------------- |
| 프론트엔드   | **브라우저 안**에서 도는 코드 (내 기기에서 실행)   |
| 호스팅       | 그 코드를 **건네주는** 서버                        |
| 백엔드       | **서버 안**에서 도는 코드                          |

즉 **프론트엔드는 서버가 아니다.** 서버가 건네준 뒤 **내 기기에서 도는 것**이다.
이 한 줄을 기억해 두면 "프론트엔드 서버"라는 말을 들어도 흔들리지 않는다.

## 4. 두 번째 벽 — "로그인이 안 되는데요"

파일을 올려 두는 것만으로는 안 되는 일이 있다.

### 파일은 기억하지 못한다

| 하고 싶은 것       | 왜 파일만으로는 안 되나                                        |
| ------------------ | --------------------------------------------------------------- |
| 로그인             | 비밀번호가 맞는지 **누가 판단하나?** HTML 파일은 판단할 줄 모른다 |
| 점수 · 진도 저장   | **어디에 저장하나?** 브라우저에 저장하면 **그 기기에만** 남는다   |
| 선생님이 반 전체 보기 | **모두의 기록이 한 곳에 모여** 있어야 한다                      |

가장 흔한 오해가 두 번째다. 브라우저에도 값을 저장할 수는 있다.
하지만 그건 **그 기기 안에만** 남는다. 태블릿을 바꾸면 사라지고, 선생님은 볼 수 없다.

### 그래서 둘이 더 필요해진다

| 필요한 것            | 하는 일                                  |
| -------------------- | ----------------------------------------- |
| **물음에 답하는 쪽** | 요청을 받아 확인하고 답을 만든다 (API 서버) |
| **기억해 두는 쪽**   | 그 답의 재료를 보관한다 (데이터베이스)      |

### 왜 데이터베이스에 바로 연결하지 않나

"기억할 곳이 필요하면 데이터베이스만 있으면 되지 않나?" 싶지만, 그러면 안 된다.
**데이터베이스에 아무나 직접 손대게 두면, 남의 기록도 마음대로 보고 고칠 수 있다.**

그래서 그 앞에 **문지기**를 세운다. 그게 API 서버다.
앱은 데이터베이스가 아니라 **API 서버에게 물어보고**, API 서버가 확인한 뒤 답한다.

## 5. 전체 흐름

```
   HTML 을 만들었다
     |
     v
   "친구에게 보여주고 싶다"
     |
     v
   [1] 파일을 건네줄 자리가 필요하다
       - 꺼지지 않는 컴퓨터. 그게 서버다
     |
     v
   [2] 거기 올려 두고 주소를 받는다
       - 이게 배포다
     |
     v
   "로그인과 점수 저장이 필요하다"
     |
     v
   [3] 물음에 답해 줄 자리가 필요하다
       - 같은 서버, 다른 일. 이게 API 서버다
     |
     v
   [4] 기억해 둘 곳이 필요하다
       - 이게 데이터베이스다
```

## 6. 그래서 이 프로젝트는 이렇게 생겼다

위 네 단계가 그대로 이 프로젝트의 구조다.

| 단계에서 필요해진 것 | 이 프로젝트에서는          |
| -------------------- | --------------------------- |
| 파일을 건네줄 자리    | **Cloudflare Pages**        |
| 물음에 답할 자리      | **Cloudflare Workers** (API) |
| 기억해 둘 곳          | **Supabase** (데이터베이스)  |

App · API · DB 로 나뉜 것은 누가 정해 준 형식이 아니라,
**하나씩 막힐 때마다 필요해서 붙인 결과**다.

구체적인 구성은 [기술 및 서비스 구성](tech-stack.md)에 있다.

## 7. 덧 — 이 앱에는 "파일을 건네주는 서버"가 없다

마지막으로 두 가지 일이 정말 다른 것이라는 가장 분명한 증거.

태블릿에 설치된 APK 안에는 **게임 파일이 통째로 들어 있다.**
그래서 파일을 건네줄 서버가 **아예 필요 없다.** 이미 기기 안에 있으니까.

그런데도 **물음에 답하는 서버는 여전히 필요하다.**

> **게임 화면은 인터넷 없이 돈다. 하지만 로그인과 진도 저장은 서버가 있어야 한다.**

같은 앱 안에서 한쪽은 서버가 필요 없고 한쪽은 필요하다.
두 가지 일이 별개라는 뜻이다. ([앱이 태블릿에서 실행되기까지](how-it-runs.md))
